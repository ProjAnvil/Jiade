apiVersion: 1
datasources:
  - name: Prometheus
    uid: Prometheus   # dashboard JSON references the datasource by uid, so it must stay fixed
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
